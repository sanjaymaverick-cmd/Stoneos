# StoneOS Windows desktop

Electron shell around the same web UI and API. It does not implement a second screen set.

```bash
npm install
npm start
npm run dist:win
```

Set `STONEOS_WEB_URL` to the deployed PWA origin in production builds.
