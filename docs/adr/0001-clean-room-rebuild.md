# Clean-room rebuild

The ston3gpt and stoneos3 repositories are reference implementations only. StoneOS is a new codebase. Auth models, migration history, Clerk, demo bypass, and Vercel adapters are not imported. Business rules (opening-count go-live, slab serials, 07:00 operational day, 105 sqft/ton recovery, owner-only owner management) are reimplemented from the audit, not copied as files.
