# Versioned REST and OpenAPI

The public API is `/api/v1`. NestJS generates OpenAPI at `/api/docs`. Shared Zod schemas in `packages/contracts` are the request/response source of truth for web, desktop, and Android.
