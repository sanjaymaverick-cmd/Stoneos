# Dual-cloud Terraform, managed PostgreSQL

OCI and AWS are both first-class. Default production database is managed PostgreSQL (OCI Database with PostgreSQL or Amazon RDS). Self-hosted Postgres is documented for local/dev and air-gapped alternatives. Secrets live in OCI Vault or AWS Secrets Manager.
