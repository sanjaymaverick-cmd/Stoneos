# One web UI, native shells

The Next.js PWA is the only application UI. Electron (Windows) and Capacitor (Android) wrap that UI and share `packages/sync-client`. There is no second React Native or desktop-specific screen set.
