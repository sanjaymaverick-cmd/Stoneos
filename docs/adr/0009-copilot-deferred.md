# Copilot deferred

AI/Copilot is out of v1. stoneos3’s SQL validator and read-only role are the intended later pattern, only after authorized factory data is flowing and a RO database role exists. UI personalization stored in localStorage is not factory truth.
